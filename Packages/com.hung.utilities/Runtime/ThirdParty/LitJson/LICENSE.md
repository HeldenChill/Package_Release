# LitJson

Public-domain JSON parser/writer for .NET. Authors disclaim copyright (see each file's header, e.g. `JsonMapper.cs`). Vendored source, no upstream version pin recorded in-repo — treat as-is.

Assembly: `Hung.Utilities.Json`.
